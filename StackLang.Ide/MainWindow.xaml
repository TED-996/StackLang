<Window x:Class="StackLang.Ide.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:sli="clr-namespace:StackLang.Ide"
        Title="StackLang.IDE" Height="768" Width="1024" ResizeMode="CanResize" WindowState="Maximized" Closing="OnWindowClosing">
	<Window.InputBindings>
		<KeyBinding Gesture="CTRL+S" Command="Save" />
		<KeyBinding Gesture="CTRL+W" Command="Close" />
		<KeyBinding Gesture="CTRL+N" Command="New" />
		<KeyBinding Gesture="CTRL+O" Command="Open" />
		<KeyBinding Gesture="F9" Command="{x:Static sli:MainWindow.RunCommand}" />
		<KeyBinding Gesture="F8" Command="{x:Static sli:MainWindow.DebugCommand}" />
	</Window.InputBindings>
	<Window.CommandBindings>
		<CommandBinding Command="Save" Executed="OnSavePressed" />
		<CommandBinding Command="Close" Executed="OnClosePressed" />
		<CommandBinding Command="Open" Executed="OnOpenPressed" />
		<CommandBinding Command="New" Executed="OnNewPressed" />
		<CommandBinding Command="{x:Static sli:MainWindow.RunCommand}" Executed="OnRunPressed" />
		<CommandBinding Command="{x:Static sli:MainWindow.DebugCommand}" Executed="OnDebugPressed" />
	</Window.CommandBindings>
	<Grid Margin="0,0,0,0">
		<Menu Width="Auto" Height="20" VerticalAlignment="Top">
            <MenuItem Header="File">
				<MenuItem Header="New (Ctrl+N)" Click="OnNewPressed" />
				<MenuItem Header="Open (Ctrl+O)" Click="OnOpenPressed" />
				<MenuItem Header="Save (Ctrl+S)" Click="OnSavePressed" />
				<MenuItem Header="Save As" Click="OnSaveAsPressed" />
				<MenuItem Header="Close (Ctrl+W)" Click="OnClosePressed"/>
				<Separator />
				<MenuItem Header="Exit" Click="OnExitPressed" />
			</MenuItem>
			<MenuItem Header="Run (F9)" Click="OnRunPressed"/>
            <MenuItem Header="Debug (F8)" Click="OnDebugPressed"/>
			<MenuItem Header="Abort" Click="OnAbortPressed"/>
        </Menu>
        <Grid Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="3*" />
                <RowDefinition />
            </Grid.RowDefinitions>

			<Border Grid.Column="0" Grid.Row="0" BorderBrush="Silver" BorderThickness="1">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="3*" />
						<ColumnDefinition Width="0" Name="DebugColumnDefinition" />
					</Grid.ColumnDefinitions>
					<TabControl Name="EditorTabs" Grid.Column="0">
					</TabControl>
					<Button Content="+" Width="24" Height="24" Grid.Column="0" Click="OnNewPressed" Margin="1"
							VerticalAlignment="Top" HorizontalAlignment="Right"/>
					<GridSplitter Grid.Column="1" Width="5" VerticalAlignment="Stretch" HorizontalAlignment="Left" 
								  Visibility="Collapsed" Name="DebugSplitter"/>
					<Grid Grid.Column="1" Margin="5,0,0,0">
						<Grid.RowDefinitions>
							<RowDefinition Height="26" />
							<RowDefinition Height="*" />
							<RowDefinition Height="5" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>
						<Label Content="Debug Information" Height="26" VerticalAlignment="Top" Background="Gray"
							   Grid.Row="0"/>
						<Border Grid.Row="1" BorderBrush="Silver" BorderThickness="1">
							<Grid>
								<Label Content="Stack" Height="26" VerticalAlignment="Top" Background="LightGray" />
								<ScrollViewer Margin="0,26,0,0" VerticalScrollBarVisibility="Visible">
									<StackPanel Name="StackVieverStackPanel">
										
									</StackPanel>
								</ScrollViewer>
							</Grid>
						</Border>
						<GridSplitter Grid.Row="2" Height="5" ResizeDirection="Rows" HorizontalAlignment="Stretch" />
						<Border Grid.Row="3" BorderBrush="Silver" BorderThickness="1">
							<Grid>
								<Label Content="Memory" Height="26" VerticalAlignment="Top" Background="LightGray" />
								<ScrollViewer Margin="0,26,0,0" VerticalScrollBarVisibility="Visible">
									<StackPanel Name="MemoryViewerStackPanel">

									</StackPanel>
								</ScrollViewer>
							</Grid>
						</Border>
					</Grid>
				</Grid>
            </Border>
			<Border Grid.Column="1" Grid.Row="0" BorderBrush="Silver" BorderThickness="1">
				<sli:ResultArea x:Name="IdeResultArea"/>
            </Border>
            <Border Grid.Column="0" Grid.Row="1" BorderBrush="Silver" BorderThickness="1">
                <!-- OUT -->
				<sli:OutputArea x:Name="OutputArea"/>
            </Border>
            <Border Grid.Column="1" Grid.Row="1" BorderBrush="Silver" BorderThickness="1">
                <!-- VARS -->
            </Border>
            

        </Grid>
    </Grid>
</Window>
